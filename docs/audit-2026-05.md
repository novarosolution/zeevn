# Codebase audit — May 2026

**Project:** Zeevan e-commerce (`zeevan-ecommerce-app`)  
**Audit date:** 2026-05-16  
**Branch created:** `chore/ui-consolidation-base` (pushed to `origin`)  
**Purpose:** Stabilize before new UI work — git hygiene, file-size audit, duplication audit, risk flags.

---

## STEP 1 — Git hygiene

### Current status

```text
On branch chore/ui-consolidation-base (tracks origin/chore/ui-consolidation-base)
Working tree: clean (no modified or untracked files)
Base: same HEAD as main — c94537e
```

**Note:** All recent auth + web UI work is **already committed on `main`**. There was nothing left to stage at audit time. The feature branch preserves that snapshot for consolidation work without touching `main` further until reviewed.

### Modified / untracked files (at audit time)

| State | Count |
|--------|--------|
| Modified | 0 |
| Untracked | 0 |

### Preserved work — where it lives

Recent auth + UI reliability work is concentrated in these commits (newest first):

| Commit | Message | Scope (summary) |
|--------|---------|-----------------|
| `c94537e` | this update the all page | **Auth + web UI hotfixes** — 30 files, +1031/−312 (see file list below) |
| `00826fe` | this are update full app | **Large UI overhaul** — 274 files, +32167/−9244 (design system, cart drawer, account shell, web header, backend auth utils, etc.) |
| `d33ed67` | update the home page | Home/category work |
| `a70354d` | refine home category tiles… | Home tiles |

#### `c94537e` — auth + web UI (preserve)

| File | Role |
|------|------|
| `App.js` | Deep link `returnTo` JSON encode/decode |
| `backend/server.js`, `backend/README.md` | CORS / docs |
| `backend/src/controllers/userController.js` | Email normalize, refresh session, forgot-password devLink |
| `src/components/ui/inputWebHelpers.js` | **New** — `WebNativeTextInput`, `WebNativeButton`, `WebTextLink`, `toWebButtonStyle`, `flattenStyleForDom` |
| `src/components/ui/webButtonLayout.js` | **New** — layout vs press style split for web buttons |
| `src/theme/webStacking.js` | **New** — z-index / elevated layers |
| `src/utils/webToastPortal.js` | **New** — toast DOM portal host |
| `src/components/ui/Button.js`, `Input.js`, `Toast.js` | Web DOM paths, fixed pill height |
| `src/components/ui/PremiumInput.js` | Shim updates for web helpers |
| `src/context/AuthContext.js`, `hooks/useAuthSubmit.js`, `hooks/useAuthScreenLifecycle.js` | Login/session UX |
| `src/screens/LoginScreen.js`, `ForgotPasswordScreen.js`, `RegisterScreen.js`, `VerifyEmailScreen.js` | Auth screens |
| `src/components/auth/AuthShell.js`, `AuthErrorCard.js`, `authNavigation.js` | Auth chrome + stacking |
| `src/components/CustomerScreenShell.js` | Content elevation above decor |
| `src/components/account/interactions/AccountSaveButton.js` | Web native save button |
| `src/navigation/AppNavigator.js` | Stack z-index baseline |
| `src/theme/web.js` | Global button CSS, z-index ladder |
| `src/content/appContent.js`, `src/services/userService.js` | Copy / API tweaks |
| `src/screens/account/AccountProfileScreen.js` | Profile sync after verify |

#### `00826fe` — full app UI (preserve; split separately if refactoring)

Includes new `src/components/ui/*` primitives, `CartDrawer`, `WebAppHeader`, `AccountLayout`, PLP layout, wishlist context, hooks, backend mail/rate-limit, `.env.example`, etc. Too large for one logical commit; treat as **baseline overhaul** when planning extractions.

### Logical commits (recommended — not applied)

Working tree was clean, so these were **not** created as separate commits. Use as a template if splitting `c94537e` (e.g. `git reset --soft` on a branch) or for future PR discipline:

1. `fix(backend): allow X-Session-Id and X-Captcha-Token in CORS; document CORS_ORIGINS`
2. `fix(auth): normalize email on login/register; preserve sessionId on token refresh`
3. `feat(auth): forgot-password devLink in non-prod when SMTP disabled`
4. `fix(web): native input/button DOM helpers; auth field typing and clicks`
5. `fix(web): returnTo deep link JSON; ignore [object Object]`
6. `fix(web): stacking contexts — webStacking, toast portal, auth elevated layers`
7. `fix(web): button pill height and flex stretch (toWebButtonStyle, webButtonLayout)`
8. `chore(auth): register screen dead verification UI cleanup; verify-email token rename`

### Branch push

| Item | Value |
|------|--------|
| Branch | `chore/ui-consolidation-base` |
| Remote | `origin/chore/ui-consolidation-base` |
| PR link | https://github.com/novarosolution/zeevn/pull/new/chore/ui-consolidation-base |

**Do not merge to `main` until** oversized-file plan and Premium→ui migration are agreed.

### Review checklist — incomplete / experimental

| File / area | Flag | Notes |
|-------------|------|--------|
| `src/components/ui/inputWebHelpers.js` | Review | Large (297+ lines); central web DOM layer — needs tests and API freeze |
| `src/theme/webStacking.js`, `src/utils/webToastPortal.js` | Review | New global stacking/toast behavior — verify all screens |
| `src/screens/LoginScreen.js` | Review | ~631 lines; raw `<button>` for Remember Me + `WebTextLink` mix |
| `noopOAuth` in `LoginScreen`, `RegisterScreen`, `AuthShell`, `AuthPageScaffold` | Experimental | OAuth buttons render but `onPress` is no-op |
| `src/screens/RegisterScreen.js` | Review | Large diff in `c94537e` (−156 lines) — confirm no dropped flows |
| `src/screens/ProfileScreen.js` vs `AccountNavigator` | Duplication | Legacy profile (~1592 lines) coexists with new account stack |
| `src/content/appContent.js` | Review | 2582 lines — copy monolith |
| `commit messages` (`this update the all page`) | Hygiene | Non-descriptive; rewrite in future PRs |
| `docs/design-sweep-report.md` | Reference | Prior design sweep; may be stale vs current UI |
| Global CSS in `applyWebPremiumChrome` | Review | `button.zv-web-button` rules — confirm no admin/modal regressions |

---

## STEP 2 — File size audit

### `src/` — files over 600 lines

| Lines | File |
|------:|------|
| 4181 | `src/screens/HomeScreen.js` |
| 2609 | `src/screens/MyOrdersScreen.js` |
| 2582 | `src/content/appContent.js` |
| 2252 | `src/components/ProductCard.js` |
| 1592 | `src/screens/ProfileScreen.js` |
| 1408 | `src/screens/account/AccountProfileScreen.js` |
| 1374 | `src/screens/admin/AdminAnalyticsScreen.js` |
| 1196 | `src/screens/CartScreen.js` |
| 1094 | `src/components/WebAppHeader.js` |
| 1053 | `src/screens/AdminScreen.js` |
| 1037 | `src/components/product/ProductPurchaseColumn.js` |
| 1028 | `src/screens/ProductScreen.js` |
| 935 | `src/screens/admin/AdminAddProductScreen.js` |
| 928 | `src/screens/admin/AdminOrdersScreen.js` |
| 800 | `src/components/product/ProductGallery.js` |
| 798 | `src/components/PremiumProductCard.js` |
| 731 | `src/screens/SupportScreen.js` |
| 667 | `src/components/account/AccountOverview.js` |
| 648 | `src/screens/EditProfileScreen.js` |
| 631 | `src/screens/LoginScreen.js` |

### `backend/src/` — files over 600 lines

| Lines | File |
|------:|------|
| 1264 | `backend/src/controllers/orderController.js` |
| 828 | `backend/src/controllers/userController.js` |
| 690 | `backend/src/controllers/analyticsController.js` |

### Extraction plans

#### `HomeScreen.js` (4181 lines)

| New module | Responsibility |
|------------|----------------|
| `src/screens/home/HomeHero.js` | Marketing hero, concierge bar, search entry |
| `src/screens/home/HomeCategories.js` | Category grid / quick nav |
| `src/screens/home/HomeReorder.js` | Reorder strip integration |
| `src/screens/home/HomeLiveOrder.js` | Pinned live order card |
| `src/screens/home/HomeProductRails.js` | Product rows, rails, carousels |
| `src/screens/home/HomeTrust.js` | Trust / quality strips |
| `src/screens/home/HomeStats.js` | Stats / social proof blocks |
| `src/screens/home/HomeTestimonials.js` | Testimonials / editorial quotes |
| `src/screens/home/HomeFooter.js` | Footer CTA, newsletter (or delegate to `HomePageFooter`) |
| `src/screens/home/useHomeScreenData.js` | Data fetching, home view config, memoized selectors |
| `src/screens/home/homeScreenStyles.js` | StyleSheet factory (largest style block) |
| `HomeScreen.js` (thin) | Compose sections, providers, scroll shell |

#### `ProductCard.js` (2252 lines)

| New module | Responsibility |
|------------|----------------|
| `src/components/product/card/ProductCardImage.js` | Image, badges, wishlist overlay |
| `src/components/product/card/ProductCardBody.js` | Title, meta, variant chips |
| `src/components/product/card/ProductCardPrice.js` | Price, MRP, offers |
| `src/components/product/card/ProductCardActions.js` | Add to cart, stepper, notify modal |
| `src/components/product/card/productCardStyles.js` | Premium vs compact style variants |
| `ProductCard.js` (thin) | Variant dispatch (`premium` / inline / legacy) |

**Note:** `PremiumProductCard.js` (798 lines) overlaps — long-term merge into split `ProductCard` or single card system.

#### `orderController.js` (1264 lines)

| New module | Exported handlers |
|------------|-------------------|
| `backend/src/controllers/orders/createOrder.js` | `createOrder` |
| `backend/src/controllers/orders/couponCart.js` | `validateCouponForCart`, `getAvailableCouponsForCart` |
| `backend/src/controllers/orders/paymentRazorpay.js` | `verifyPayment`, `razorpayWebhook`, `cancelPendingOrder`, `sweepExpiredPendingPayments` |
| `backend/src/controllers/orders/reorder.js` | `reorderMyOrder`, `claimMyOrderReward` |
| `backend/src/controllers/orders/myOrders.js` | `getMyOrders`, `updateMyOrderAddress` |
| `backend/src/controllers/orders/adminOrders.js` | `getAllOrders`, `updateOrderStatus`, `updateAdminOrderDetails`, `deleteOrder` |
| `backend/src/controllers/orders/delivery.js` | `updateMyDeliveryLocation`, `getMyDeliveryOrders`, `markMyDeliveryOrderDelivered`, `getMyOrderLiveLocation`, `getMyOrderDrivingRoute` |
| `backend/src/controllers/orders/orderHelpers.js` | `liveLocationDestinationSummary`, `getRequestSubtotal`, `getUserCartSubtotal`, shared validators |
| `orderController.js` (thin) | Re-export from above for route imports |

#### Other high-priority splits (not in original prompt)

| File | Suggested split |
|------|-----------------|
| `appContent.js` (2582) | Domain files: `authContent.js`, `cartContent.js`, `homeContent.js`, `accountContent.js`, barrel `appContent.js` |
| `MyOrdersScreen.js` (2609) | List, filters, detail sheet, styles, hooks |
| `WebAppHeader.js` (1094) | Search, nav, cart badge, drawer trigger, styles |
| `userController.js` (828) | `auth.js`, `profile.js`, `sessions.js`, `cart.js`, `notifications.js` |

---

## STEP 3 — Duplication audit (UI primitives)

### Shim architecture

Most `Premium*` components are **thin wrappers** around `src/components/ui/*`:

| Legacy | Wraps | Implementation file |
|--------|--------|---------------------|
| `PremiumButton` | `Button` | `PremiumButton.js` → `Button.js` |
| `PremiumInput` | `Input` | `PremiumInput.js` → `Input.js` |
| `PremiumEmptyState` | `EmptyState` | `PremiumEmptyState.js` → `EmptyState.js` |
| `PremiumLoader` | `ProgressRing` (approx.) | `PremiumLoader.js` |
| `PremiumCard`, `PremiumChip`, etc. | Partial / parallel | Own files — not all unified |

**Web-only layer:** `inputWebHelpers.js` — `WebNativeButton`, `WebNativeTextInput`, `WebTextLink`, `toWebButtonStyle` (used by `Button`, `Input`, auth screens, `AccountSaveButton`).

### Migration table

Consumer counts = files that import the symbol (excluding the shim file itself and `ui/index.js` barrel). Counts are approximate from static import scan.

| Primitive | New path | Legacy path | New consumers | Legacy consumers | Migration notes |
|-----------|----------|-------------|---------------:|-----------------:|-----------------|
| **Button** | `components/ui/Button.js` | `components/ui/PremiumButton.js` | **38** | **22** | Premium is shim; migrate imports to `Button`, keep variant mapping at call site |
| **Input** | `components/ui/Input.js` | `components/ui/PremiumInput.js` | **13** | **15** | Auth uses `Input` directly; admin flows still mostly Premium |
| **Card** | `components/ui/Card.js` | `components/ui/PremiumCard.js` | **40** | **19** | PremiumCard has extra props; audit before bulk replace |
| **EmptyState** | `components/ui/EmptyState.js` | `components/ui/PremiumEmptyState.js` | **12** | **13** | Premium is deprecated wrapper — trivial rename migration |
| **Toast** | `components/ui/Toast.js` | _(none)_ | **8** | **0** | Single implementation |
| **Screen** | `components/ui/Screen.js` | _(none)_ | **14** | **0** | Editorial/PLP/account |
| **PageHeader** | `components/ui/PageHeader.js` | _(none)_ | _(low)_ | — | Used in scaffold screens |
| **SectionHeader** | `components/ui/SectionHeader.js` | `components/ui/PremiumSectionHeader.js` | **12** | **11** | `HomeSectionHeader.js` re-exports Premium |
| **Badge** | `components/ui/Badge.js` | _(none)_ | _(low)_ | — | |
| **Skeleton** | `components/ui/Skeleton.js` | `components/ui/SkeletonBlock.js` | **~8** | **legacy block** | Two skeleton APIs coexist |
| **ProgressRing** | `components/ui/ProgressRing.js` | `components/ui/PremiumLoader.js` | **5** | **10** | PremiumLoader used heavily in admin |
| **Web button** | `inputWebHelpers` → `WebNativeButton` | Raw `<button>` | **via Button** | **1** (`LoginScreen` Remember Me) | Consolidate Remember Me to shared checkbox component |

### Legacy import hotspots (Premium*)

| Legacy | Top consumers |
|--------|----------------|
| `PremiumButton` | Admin screens, `MyOrdersScreen`, `ProfileScreen`, `SupportScreen`, `RedeemRewardsScreen`, `AuthGateShell`, `OrderLiveMapCard.*` |
| `PremiumInput` | Admin CRUD, `EditProfileScreen`, `ManageAddressScreen`, `SupportScreen` |
| `PremiumCard` | Admin, `MyOrdersScreen`, `ProfileScreen`, `SupportScreen`, `HomeReorderStrip` |
| `PremiumEmptyState` | Admin lists, `HomeScreen`, `MyOrdersScreen`, `SupportScreen` |
| `PremiumLoader` | Admin dashboards, `MyOrdersScreen`, `EditProfileScreen`, `AdminScreen` |
| `PremiumChip` | Admin filters, `ProfileScreen`, `MyOrdersScreen` |
| `PremiumErrorBanner` | Widespread admin + profile flows |
| `PremiumStatCard` | `AdminAnalyticsScreen`, `MyOrdersScreen`, `ProfileScreen`, `RedeemRewardsScreen` |
| `PremiumSwitch` | `SettingsScreen`, `AccountProfileScreen`, admin toggles |
| `PremiumStickyBar` | `EditProfileScreen`, `ManageAddressScreen` |
| `PremiumConfirmDialog` | `AdminOrdersScreen`, `AdminUsersScreen` |

### Direct `Button` adopters (preferred path)

Account, auth, cart, editorial, ops: e.g. `LoginScreen`, `CartScreen`, `AuthShell`, `CartDrawer`, `AccountOverview`, modals under `components/account/**`.

### Recommended migration order

1. **EmptyState** — rename imports only (shim already delegates).
2. **Button** — replace `PremiumButton` in admin (22 files); verify `danger` → `destructive` mapping via shim props.
3. **Input** — auth already on `Input`; migrate admin forms.
4. **Card / SectionHeader** — after API diff review.
5. **Loader** — align `PremiumLoader` with `ProgressRing` or single `Loader` export.
6. Remove deprecated Premium files once consumer count = 0.

---

## Risk flags

| Risk | Severity | Detail |
|------|----------|--------|
| Monolithic commits on `main` | High | `00826fe` + `c94537e` hard to review/revert selectively |
| `HomeScreen` / `ProductCard` size | High | Merge conflict magnet; blocks UI velocity |
| Dual profile UX | Medium | `ProfileScreen` + `AccountNavigator` / legacy redirects |
| OAuth UI without backend | Medium | `noopOAuth` — misleading on login/register |
| Web-only DOM helpers | Medium | Behavior diverges web vs native — test both on every auth/cart change |
| `appContent.js` monolith | Medium | Copy changes risky |
| Dead / legacy routes | Low | `EditProfile`, `MyOrders`, `Settings` redirect to Profile stack — still linked from old `ProfileScreen` |
| README outdated | Low | Still describes 4-screen starter |
| Secrets in `.env` | Ops | `backend/.env` must not be committed (user local only) |

---

## Next actions (no code in this audit)

1. Open PR from `chore/ui-consolidation-base` for review only (or reset-split `c94537e` per recommended commits).
2. Freeze `inputWebHelpers` API; add smoke tests for login web buttons/inputs.
3. Schedule `HomeScreen` extraction sprint before more home UI.
4. Run Premium→ui migration script starting with `PremiumEmptyState` / `PremiumButton`.
5. Split `orderController.js` when touching checkout/payment next.

---

*Generated as part of stabilization audit. No application code was modified; only this document and branch `chore/ui-consolidation-base` were created.*
