/**
 * @deprecated Import from `theme/tokens.js` or `useTheme()` aliases (`c`, `S`, `R`, `SH`, `T`, `M`).
 */
export {
  COLORS,
  COLORS_DARK,
  getSemanticPalette,
  RADII,
  SPACING,
  SHADOWS,
  TYPE,
  MOTION,
} from "../theme/tokens";
